#!/bin/sh
set -e
set -u

# for MOBILE_DIR
. "${ZDOTDIR:-$HOME/.config/zsh}"/11_locals.zsh


# should already be in opus format
p="$($HOME/.local/bin/rmusic.py)"

# tgt="$MOBILE_DIR"/music/auto/
tgt="$HOME/00_09_meta/01_syncthing/mobile_music"
mkdir -p "$tgt"
cp -r -n "$p" "$tgt"
